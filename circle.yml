test:
  override:
    - npm run coverage:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml

deployment:
  prod:
    branch: master
    heroku:
      appname: recipeek
  staging:
    branch: develop
    heroku:
      appname: recipeek-staging